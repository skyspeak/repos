{
  "name": "disruptor",
  "version": "1.0.0",
  "description": "AI disruption heatmap and investment document analyzer",
  "license": "MIT",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "node scripts/build-vercel.mjs",
    "build:vercel": "node scripts/build-vercel.mjs",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "dev:api": "PORT=5080 pnpm --filter @workspace/api-server run dev",
    "dev:app": "PORT=5180 BASE_PATH=/ API_URL=http://localhost:5080 pnpm --filter @workspace/heatmap run dev",
    "dev": "pnpm run dev:api & pnpm run dev:app",
    "db:push": "pnpm --filter @workspace/db run push",
    "codegen": "pnpm --filter @workspace/api-spec run codegen",
    "categories:export": "pnpm --filter @workspace/scripts run export-categories",
    "categories:regenerate": "pnpm --filter @workspace/scripts run regenerate-categories",
    "deploy": "vercel --prod"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
